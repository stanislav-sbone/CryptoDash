# 💰 CryptoDash

**CryptoDash** — современное веб-приложение для отслеживания курсов криптовалют и просмотра детальной информации о токенах. Приложение использует CoinGecko API для получения актуальных данных о криптовалютах категории Layer-1.

## ✨ Основные возможности

### 📊 Просмотр криптовалют

- **Список токенов**: Отображение популярных Layer-1 криптовалют с актуальными ценами
- **Фильтрация**: Поиск криптовалют по названию в реальном времени
- **Избранное**: Добавление монет в избранное для быстрого доступа
- **Вкладки**: Переключение между всеми токенами и избранными

### 📈 Детальная информация

- **График цены**: Визуализация изменения цены за последние 7 дней с использованием Chart.js
- **Текущая цена**: Отображение актуальной стоимости в USD
- **Изменения цены**: Процентные изменения за различные периоды (24ч, 7д, 30д, 60д, 200д, 1г)
- **Рыночные данные**: Капитализация, предложение токенов, максимальная цена за всё время (ATH)
- **Дневные экстремумы**: Максимальная и минимальная цена за последние 24 часа

### 🎨 Персонализация

- **Темная/Светлая тема**: Переключение между темами с сохранением выбора
- **Мультиязычность**: Поддержка русского и английского языков
- **Адаптивный дизайн**: Оптимизация для мобильных устройств, планшетов и десктопов

### 🔔 Уведомления

- **Модальное окно**: Предупреждение при первом посещении о лимитах бесплатного API

## 🛠 Технологический стек

### Основные технологии

- **React 19** — библиотека для построения пользовательского интерфейса
- **TypeScript** — типизированный JavaScript для повышения надежности кода
- **Vite** — быстрый сборщик и dev-сервер
- **Tailwind CSS 4** — utility-first CSS фреймворк для стилизации

### Управление состоянием и данными

- **Zustand** — легковесная библиотека для управления глобальным состоянием
- **TanStack Query (React Query)** — мощная библиотека для работы с серверными данными, кеширования и синхронизации

### UI компоненты и визуализация

- **Ant Design** — набор UI компонентов (иконки, спиннеры)
- **Chart.js + react-chartjs-2** — библиотека для построения графиков

### HTTP клиент

- **Axios** — HTTP клиент для запросов к API

### Инструменты разработки

- **ESLint** — линтер для проверки качества кода
- **Prettier** — форматировщик кода
- **Husky** — Git hooks для автоматизации
- **lint-staged** — запуск линтера только для измененных файлов

## 📦 Установка и запуск

### Требования

- Node.js (рекомендуется версия 18 или выше)
- pnpm (менеджер пакетов)

### Установка зависимостей

```bash
pnpm install
```

### Запуск в режиме разработки

```bash
pnpm dev
```

Приложение будет доступно по адресу `http://localhost:5173`

### Сборка для продакшена

```bash
pnpm build
```

### Предпросмотр продакшен сборки

```bash
pnpm preview
```

### Проверка кода линтером

```bash
pnpm lint
```

## 📁 Структура проекта

```
CryptoDash/
├── public/                 # Статические файлы
│   ├── assets/            # Изображения и ресурсы
│   └── logo.png           # Логотип приложения
├── src/
│   ├── components/         # React компоненты
│   │   ├── AlertModal/    # Модальное окно с предупреждением
│   │   ├── CoinDetails/   # Компоненты детальной информации о монете
│   │   ├── CoinList/      # Компоненты списка криптовалют
│   │   ├── common/        # Общие компоненты (Loading, Error)
│   │   └── Header/        # Шапка приложения
│   ├── constants.ts       # Константы приложения
│   ├── hooks/             # Кастомные React хуки
│   ├── locales/           # Переводы интерфейса
│   ├── services/          # Сервисы для работы с API
│   ├── store/             # Zustand stores для управления состоянием
│   ├── types/             # TypeScript типы и интерфейсы
│   ├── utils/             # Вспомогательные функции
│   ├── App.tsx            # Главный компонент приложения
│   ├── main.tsx           # Точка входа
│   └── index.css          # Глобальные стили
├── eslint.config.js       # Конфигурация ESLint
├── package.json           # Зависимости и скрипты
├── tsconfig.json          # Конфигурация TypeScript
└── vite.config.ts         # Конфигурация Vite
```

## 🏗 Архитектура приложения

### Управление состоянием (Zustand Stores)

Приложение использует несколько Zustand stores с персистентностью:

- **useCoinStore** — хранение ID выбранной криптовалюты
- **useFavoriteCoinsStore** — управление списком избранных монет
- **useFilterStore** — состояние фильтра поиска
- **useThemeStore** — текущая тема (light/dark)
- **useLanguageStore** — выбранный язык интерфейса
- **useAlertModalStore** — состояние модального окна предупреждения

### Работа с данными (TanStack Query)

- **Кеширование**: Данные кешируются на 30 секунд (`staleTime`)
- **Автоматическая повторная попытка**: 3 попытки при ошибке запроса
- **Оптимизация**: Отключена автоматическая перезагрузка при фокусе окна

### API интеграция

Приложение использует **CoinGecko API** (бесплатный план):

- `/markets` — получение списка криптовалют категории Layer-1
- `/{id}` — детальная информация о конкретной монете
- `/{id}/market_chart` — исторические данные цены за 7 дней

**Важно**: Из-за использования бесплатного плана API существуют ограничения на количество запросов в минуту. При быстром переключении между монетами могут возникать ошибки сети.

## 🎯 Основные компоненты

### CoinList

Компонент списка криптовалют с возможностью:

- Фильтрации по названию
- Переключения между всеми токенами и избранными
- Отображения скелетонов загрузки
- Обработки ошибок

### CoinDetails

Компонент детальной информации о выбранной криптовалюте:

- Заголовок с названием, символом и рангом по капитализации
- График изменения цены за 7 дней
- Информационная панель с изменениями цены за различные периоды
- Дополнительные метрики (капитализация, предложение, ATH)

### CoinCard

Карточка криптовалюты в списке:

- Изображение монеты
- Название и символ
- Текущая цена
- Изменение цены за 24 часа с цветовой индикацией
- Кнопка добавления в избранное

### Header

Шапка приложения с:

- Логотипом
- Кнопкой открытия модального окна предупреждения
- Переключателем языка
- Переключателем темы
- Адаптивным бургер-меню для мобильных устройств

## 🌐 Интернационализация

Приложение поддерживает два языка:

- **Русский** (ru)
- **English** (en)

## 🎨 Темы оформления

- **Светлая тема**: Светлые цвета фона с темным текстом
- **Темная тема**: Темные цвета фона со светлым текстом (тема по умолчанию)

Выбор темы сохраняется в localStorage и применяется при следующем посещении.

## 📱 Адаптивность

Приложение полностью адаптировано для различных размеров экранов:

- **Мобильные устройства** (< 640px): Вертикальная компоновка, бургер-меню
- **Планшеты** (640px - 1024px): Гибридная компоновка
- **Десктопы** (> 1024px): Горизонтальная компоновка с боковой панелью списка

## 🔧 Особенности реализации

- **SOLID принципы**: Код следует принципам SOLID для обеспечения поддерживаемости
- **KISS принцип**: Простота и понятность кода
- **TypeScript**: Строгая типизация для предотвращения ошибок
- **Компонентный подход**: Модульная архитектура с переиспользуемыми компонентами
- **Оптимизация производительности**: Мемоизация вычислений, оптимизация запросов
- **Обработка ошибок**: Graceful handling ошибок загрузки данных
- **Accessibility**: Семантическая разметка и доступность интерфейса

## 👨‍💻 Разработка

При разработке используются:

- Pre-commit hooks (Husky) для автоматической проверки кода
- ESLint для статического анализа
- Prettier для форматирования кода
- TypeScript для типобезопасности
